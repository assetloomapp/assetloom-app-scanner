# website

Astro Starlight site for AssetLoom App Scanner. The markdown content lives in
`../docs/public/` — edit pages there; this package only renders them.

```bash
pnpm --filter website dev      # local dev server
pnpm --filter website build    # static build to website/dist/
```
